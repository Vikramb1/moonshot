'use client';

/**
 * PriceLine — no longer used as a separate component.
 * Price line functionality is now inline in Game.tsx as PriceLineIndicator.
 * This file kept for backwards compatibility with any imports.
 */

export default function PriceLine() {
  return null;
}
